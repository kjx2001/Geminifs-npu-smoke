# 整体迁移已落地：`snvme-5.10-npu` 目录说明书 + 服务器实操手册

> 日期：2026-06-04
> 我已经按"整目录拷贝 + 直接改好 host-only 部分"的思路，**创建好了一个可以明天直接拷到 NPU 服务器的目录**：
> `Geminifs_smoke/backends/local/kernel_modules/snvme-5.10-npu/`
> 本文说明：这个目录是什么、我替你改了哪些地方（每处都有原因）、已经验证到什么程度、以及你明天在服务器上要做的事（编译→修报错→加载→跑测试）。
> 前置阅读：`01测试分析以及后续迁移计划.md`（能力阶梯/逐函数注释）、`02具体迁移步骤.md`（分阶段思路）。本文是 **02 的"已动手实现版"**。

---

## 0. 先回答你的问题：这一层和 CANN 有关系吗？

**没有关系。** 这一层是**内核 NVMe 驱动**（SNVMe 内核模块，`.ko`），跑在内核态，干的是"绑定 NVMe、建队列、敲 doorbell、收发数据"。

CANN（AscendCL / Ascend C）是**用户态的 NPU 计算运行时**，要等到"线 B"——把数据缓冲/队列搬上 NPU HBM、让 AIV 核敲 doorbell——而且是用户态那条线（仓库里 `Geminifs/ascend_cann_port/` 是它的工作区）才会用到。

> 本次做的（①~⑤ 纯 CPU + host 内存 + 内核驱动 bring-up）：**从头到尾不碰 CANN，也不需要装 CANN。** 只需要：openEuler 内核头（kernel-devel）+ gcc/make + 一块可清空的 NVMe 测试盘。

---

## 1. 我替你建好的目录是什么

```
Geminifs_smoke/backends/local/kernel_modules/snvme-5.10-npu/
```
它是 **`snvme-5.15.0-public`（B3 模块）的完整副本**，外加我做的 **host-only 改造**，目的是让它在**没有 NVIDIA 的机器上也能编译、能加载**。

相对原始 5.15 基线，我**只动了 3 个地方**（其余 23 个文件逐字节不变，方便你将来对照）：

| 文件 | 动作 | 为什么 |
|---|---|---|
| `pci.c` | **改 1 处**（模块初始化 `nvme_init`） | 原代码找不到 `nvidia_p2p_*` 符号就 `-EOPNOTSUPP` 拒绝加载；改成失败时打 warning、继续进 host-only 模式 |
| `nv-p2p.h` | **新增**（host-only 桩头） | 原 `nvfs-p2p.c`/`map.c` 要 `#include "nv-p2p.h"`（NVIDIA SDK 的头），NPU 服务器没有，会编译失败。补一份最小桩头让它编过 |
| `Makefile` | **新增**（独立编译入口） | 原 `Makefile.in` 有 CMake 占位符；新增一个不依赖 CMake、`make` 就能编的版本 |

> 这 3 处改动都是**与内核版本无关**的（host-only / 编译脚手架），所以可以由我离线先做好。

### 我已经验证到什么程度（重要、诚实说明）

✅ **我在本机（恰好也是 5.15 内核）用 `make` 编译了 `snvme-5.10-npu`，`snvme-core.ko` 和 `snvme.ko` 都成功产出、零报错。**
这证明：(1) 桩头 `nv-p2p.h` 够用、能在无 NVIDIA 下编过；(2) `pci.c` 的 host-only 改动语法正确；(3) `Makefile` 能用。

⚠️ **但这是对着 5.15 头编的，不等于在 openEuler 5.10 上能直接编过。**
5.15 → 5.10 之间内核 API 有差异（见第 3 步），这部分**我无法离线替你改**——必须对着你服务器上**真实的 5.10 内核头**按编译报错改，盲改只会帮倒忙。所以：**这个目录帮你跨过了"NVIDIA/host-only"这道坎，剩下的是"5.10 内核 API 适配"那道坎，要在服务器上做。**

> 我已经把本地编译产物（`*.ko/*.o` 等）清理掉了，目录里只有源码 + 我加的 3 个文件，干净可拷。

---

## 2. 明天在 NPU 服务器上：第 1 步——拷贝过去

把**整棵 `Geminifs_smoke` 树**拷到 NPU 服务器（最省心，相对路径都不会断），或至少保证下面两处的相对关系不变：
```
Geminifs_smoke/backends/local/kernel_modules/snvme-5.10-npu/      ← 模块（我改好的）
Geminifs_smoke/backends/local/nvme/libnvm/include/ioctl.h         ← UAPI 头（模块和测试都要）
Geminifs_smoke/backends/local/kernel_modules/test/                ← 6 个测试
```
> `snvme-5.10-npu/Makefile` 里用相对路径 `../../nvme/libnvm/include` 找 UAPI 头，所以**只要整棵树一起拷，路径就成立**，不用改。

服务器准备（openEuler）：
```bash
uname -r                                   # 记下，确认是 5.10.x
uname -m                                   # 记下架构（aarch64 还是 x86_64）
sudo yum install -y kernel-devel-$(uname -r) gcc make   # 装内核头和工具链
ls /lib/modules/$(uname -r)/build          # 必须存在
lspci -D -d ::0108                          # 记下 NVMe 测试盘的 BDF
```

---

## 3. 第 2 步——编译，按报错修 5.10 API（这是你明天的主要工作）

```bash
cd Geminifs_smoke/backends/local/kernel_modules/snvme-5.10-npu
make 2>&1 | tee build.log
```

会出现两种情况：

**情况 A：直接编过（运气好，openEuler backport 够多）**
→ 出现 `snvme-core.ko` 和 `snvme.ko`，直接跳到第 4 步。

**情况 B：报一批 5.15→5.10 的内核 API 错（更可能）**
→ **逐个按报错改**。方法（比清单更重要）：
1. 看报错说**哪个函数/字段"未声明 / 不是成员 / 参数不对"**。
2. 去服务器上你已有的那份原生驱动 `kernel-openEuler-22.03-LTS-drivers-nvme/drivers/nvme/host/` 里 `grep` 这个名字，看 **5.10 里它叫什么、签名是什么、字段在不在**。
3. 照 5.10 的写法把 `snvme-5.10-npu/` 里对应的地方改过来，**每改一处加一行注释**：
   ```c
   /* [SNVME-NPU 迁移修改] 5.15→5.10：xxx 在 5.10 里是 yyy，原因…… */
   ```

**可能（不一定全中，以编译器实际报错为准）遇到的高频差异：**

| 报错关键字 | 5.15 写法 | openEuler 5.10 大概率写法 | 主要出现在 |
|---|---|---|---|
| `blk_mq_alloc_disk` 未声明 | `blk_mq_alloc_disk()` | `alloc_disk()` + `blk_mq_init_queue()` + `device_add_disk()`（5.14 才合并成一个） | `core.c` |
| `set_capacity_and_notify` 未声明 | `set_capacity_and_notify()` | `set_capacity()` + `revalidate_disk_size()` | `core.c` |
| `nvme_init_ctrl_finish` 未声明 | `nvme_init_ctrl_finish()` | `nvme_init_identify()`（`snvme-rename.sed` 注释也提过） | `core.c`/`pci.c` |
| `BIO_MAX_VECS` 未声明 | `BIO_MAX_VECS` | `BIO_MAX_PAGES`（5.12 改名） | 各处 |
| `struct nvme_ctrl`/`nvme_dev` 某字段不存在 | 5.15 的字段 | 对照 5.10 `nvme.h` 改名/删 | `pci.c`/`core.c` |

> **改不动、看不懂某处"5.15 为什么这么写"时**，才对那一个文件做三方 diff（拿原生 5.15 的 `drivers/nvme/host/同名文件` 和 `snvme-5.15.0-public/同名文件` 对比，看 SNVMe 到底改了哪几处），把那几处看懂再套到 5.10。**不要一上来就全文件三方合并**——大多数错照着报错改就行。
>
> **备选方案**：openEuler 5.10 夹在仓库的 5.4 和 5.15 两个 baseline 中间，而 `blk_mq_alloc_disk` 这类磁盘分配大改是 5.14 才进的。**如果 5.15 起手报了一大堆磁盘分配/probe 相关的错，可以改用 `snvme-5.4.241-1-tlinux4-0017` 作为起点**（它也实现了完整 B3），它的 nvme 内核内部可能更接近 5.10。换 baseline 时，把我对 `snvme-5.10-npu` 做的同样 3 处 host-only 改动照搬过去即可（见第 6 节"我的改动清单"）。

编过的判据：
```bash
ls -l snvme-core.ko snvme.ko       # 两个都在 = 第 2 步完成
```

---

## 4. 第 3 步——加载模块

```bash
sudo insmod snvme-core.ko
sudo insmod snvme.ko
ls -l /dev/snvm_control            # 出现（mode 0666）= 加载成功
dmesg -T | tail -40               # 应能看到我加的那条 host-only warning：
                                   #   "snvme: nvidia_p2p* symbols not found; running in HOST-ONLY mode ..."
```
看到那条 warning = host-only 改动生效、模块正常进入纯 CPU 模式。

常见加载失败：
- `Required key not available` → 内核开了强制模块签名。`grep CONFIG_MODULE_SIG_FORCE /boot/config-$(uname -r)` 确认；要么走签名流程，要么换一台没开强制签名的开发机（PORTING.md §6.1）。
- `Unknown symbol …` → 还有没改名、和系统自带 `nvme` 冲突的导出符号；按 `dmesg` 指出的符号名继续修（改名脚本是 `snvme-5.4.241-1-tlinux4-0017/snvme-rename.sed`，但 5.10 的导出符号要重新核对）。

卸载：
```bash
sudo rmmod snvme && sudo rmmod snvme_core
```

---

## 5. 第 4 步——按顺序跑 6 个测试

```bash
cd ../test
make                               # 没 nvcc 会自动跳过 snvme_smoke_gpu
BDF=0000:08:00.0                   # 改成你的实际 BDF

# 先安全（不 bind，最先该绿）
sudo dmesg -C; sudo ./snvme_smoke_qgroup $BDF ; sudo dmesg -T | tail -30   # ① chrdev+queue group+host map
sudo dmesg -C; sudo ./snvme_smoke        $BDF ; sudo dmesg -T | tail -30   # ② BAR0 mmap + host SQ/CQ

# 再破坏性（bind/写盘，用测试盘）—— probe 路径是 5.10 移植的硬骨头
sudo dmesg -C; sudo ./snvme_smoke_recycle $BDF ; sudo dmesg -T | tail -50  # ③ bind/probe + admin 透传
sudo dmesg -C; sudo ./snvme_smoke_addq    $BDF ; sudo dmesg -T | tail -50  # ④ 注册用户 I/O queue
sudo dmesg -C; sudo ./snvme_smoke_io      $BDF ; sudo dmesg -T | tail -50  # ⑤ CPU 全程读写校验
# ⑥ snvme_smoke_gpu：host-only 跳过（要 NVIDIA），NPU 阶段再做
```

**判读 + 哪挂查哪**（详见 `01…md` 第 2 节、`02…md` 第 0 节的能力表）：
- 成功末尾打印 `all N steps passed`；失败看**最后一行 `[FAIL] step=N`**。
- ①② 不 bind，最先该绿（说明 chrdev/map/queue group 这层在 5.10 OK）。
- ③④⑤ 要 bind，会走 `pci.c` 的 probe 路径——**5.10 移植没改干净的话最可能挂在 ③**（bind 后 `NVM_GET_DEV_INFO` 超时 / dmesg 报 probe 错），回 `pci.c` 的 `s_nvme_probe`/`reset_work`/admin queue 对照 5.10 改。
- **aarch64 注意**：⑤ 若出现 CQE 超时/数据偶发错，重点查 `snvme_smoke_io.c` 的 `mmio_writel`（doorbell 写序，x86 是 sfence，arm 走 `__atomic_thread_fence`，可能需要更强 barrier）。

①~⑤ 全绿 = **M1 达成（纯 CPU 能力阶梯在 openEuler 上通过，不依赖华为）**。

---

## 6. 我对 `snvme-5.10-npu` 的改动清单（精确，便于复查/回退/换 baseline 时照搬）

### 改动 #1：`pci.c` — 模块初始化进 host-only
位置：`nvme_init()`（原 5.15 基线约 6066 行）。
```c
/* 原始： */
if(nvfs_nvidia_p2p_init()) {
    printk("Could not load nvidia_p2p* symbols\n");
    ret = -EOPNOTSUPP;
    return ret;
}
/* 改成（带 [SNVME-NPU 迁移修改 #1] 注释块）： */
if(nvfs_nvidia_p2p_init()) {
    printk(KERN_WARNING "snvme: nvidia_p2p* symbols not found; "
           "running in HOST-ONLY mode "
           "(device-memory ioctls will return -ENOMEM/-EOPNOTSUPP)\n");
    /* 不再 return；继续加载 */
}
```
原因：没有 NVIDIA 时也能加载，host 内存路径照常工作；GPU/device 路径由 `nvfs-p2p.c` 的包装函数在指针为 NULL 时自动返回 `-ENOMEM`（已确认不会崩）。

### 改动 #2：新增 `nv-p2p.h`（host-only 桩头）
原因：`nvfs-p2p.c`/`nvfs-p2p.h`/`map.c` 都 `#include "nv-p2p.h"`，缺它编译失败。桩头只定义代码实际用到的两个类型：
- `struct nvidia_p2p_page_table { u32 entries; }`（map.c 读 `->entries`）
- `struct nvidia_p2p_dma_mapping { u64 *dma_addresses; }`（map.c 读 `->dma_addresses[i]`）
+ 两个 `_t` typedef。host-only 运行时根本走不到读这些字段的 device 路径，字段只为编译存在。

### 改动 #3：新增 `Makefile`（独立编译入口）
原因：绕开 `Makefile.in` 的 CMake 占位符，`make` 直接编。对象集与原始一致；`-I` 指向 UAPI 头和本目录。

> **换用 5.4 baseline 时**：把这 3 处同样套到 `snvme-5.4.241` 的副本上即可（pci.c 的初始化处函数名/行号可能不同，但逻辑一样：找不到 nvidia 符号别 return，继续加载）。

### 一句话验证我没乱改
```bash
cd backends/local/kernel_modules
diff -rq snvme-5.15.0-public snvme-5.10-npu
# host-only 批次（第 1~6 节）：Makefile / nv-p2p.h 新增 + pci.c differ
# 叠加 5.10 API 批次（第 8 节）后：还会多出 core.c / multipath.c differ
```

---

## 7. 一页速查

```
目录：snvme-5.10-npu/ = snvme-5.15.0-public 副本 + 3 处 host-only 改动（已本地 5.15 编译通过）
      改动：pci.c(不再因缺 NVIDIA 拒绝加载) + nv-p2p.h(桩头) + Makefile(独立编译)

服务器上：
  1 拷贝   整棵 Geminifs_smoke 拷过去（保相对路径）
  2 编译   cd snvme-5.10-npu && make
           └─ 报 5.15→5.10 API 错 → 对照机器上 openEuler 原生 nvme/host 按报错改（每处加注释）
           └─ 错太多且集中在磁盘分配/probe → 改用 snvme-5.4.241 baseline 重试（把 3 处改动照搬）
  3 加载   insmod snvme-core.ko && insmod snvme.ko → 看 /dev/snvm_control + host-only warning
  4 测试   cd ../test && make
           ① qgroup ② smoke（不bind，先绿）→ ③ recycle ④ addq ⑤ io（bind/写盘，probe硬骨头）→ ⑥ gpu 跳过
  里程碑：①~⑤ 全绿 = M1（纯 CPU，不靠华为，不碰 CANN）
```

> CANN 与本阶段**完全无关**——这是内核 NVMe 驱动层。CANN 是后续"线 B"（数据/队列上 NPU HBM、AIV 敲 doorbell）的用户态运行时，那是 M1 之后、且要华为开放 peer-DMA/AIV 能力才做的事。

---

## 8.【进度更新 2026-06-05】5.10 内核 API 适配（已替你修一批）

> 你已在华为服务器上确认：`uname -r=5.10.0`、`uname -m=aarch64`，并 `make` 出现一批 5.15→5.10 的内核 API 报错。**我已用 openEuler 5.10 自带的 nvme 驱动作权威参照，把这批改好了**，并保证本机 5.15 仍能编过（回归验证）。你 pull 后重新 `make` 即可继续。

### 8.1 方法论（很关键）：用 openEuler 自带 nvme 驱动当"标准答案"
我没有凭内核版本号猜，而是对每个报错符号，去你机器上那份 `kernel-openEuler-22.03-LTS-drivers-nvme/drivers/nvme/host/` 看 **openEuler 5.10 自己怎么写**，照抄。这避免两类坑：
- **openEuler 有 backport**：`blk_mq_alloc_disk`、`blk_cleanup_disk` 本是 5.14/5.15 才有的，**openEuler 5.10 已 backport**（它自己 nvme 驱动在用），所以这两处 snvme 代码**不用改**——也解释了你为何没报这两个错。
- **openEuler 没 backport**：`blk_alloc_disk`、`set_capacity_and_notify`、`bvec_virt`、`disk_live`、`struct nvme_id_ctrl_nvm`、`max_zeroes_sectors` 字段等，5.10 确实没有，必须改。

所有改动都用 `#if LINUX_VERSION_CODE < KERNEL_VERSION(5,15,0)` 条件编译包裹：**openEuler 5.10 走新写法，原 5.15 代码保留在 `#else`**。这样代码**同时能在 5.10 和 5.15 上编译**，我也能用本机 5.15 持续回归。

### 8.2 本批改动清单（3 个文件，均带 `[SNVME-NPU]` 注释）

| 报错符号 | 文件:位置 | 5.10 改法（依据 openEuler 5.10） |
|---|---|---|
| `set_capacity_and_notify` | core.c:138 / 1847 | → `set_capacity_revalidate_and_notify(disk, size, false)` |
| `blk_mq_set_request_complete` | core.c:393 | → 内联 `WRITE_ONCE(req->state, MQ_RQ_COMPLETE)`（该辅助 5.13+ 的实现本体） |
| `bvec_virt` | core.c:974 | → `page_address(bv_page) + bv_offset` |
| `blk_execute_rq`（返回值+签名） | core.c:1050 | 5.10：`blk_execute_rq(q, disk, rq, at_head)` 返回 void，状态从 `nvme_req(rq)` 读 |
| `blk_execute_rq_nowait`（签名） | core.c:1277 / pci.c:1485 / pci.c:2710 | 5.10 多首参 q：`blk_execute_rq_nowait(rq->q, NULL, rq, at_head, done)` |
| `disk_live` | core.c:1860 | → `(disk->flags & GENHD_FL_UP)` |
| `nvme_id_ctrl_nvm` + `max_zeroes_sectors` | core.c:2866 / 4620 / 1851 | 5.10 内核头无此结构体、`struct nvme_ctrl` 无此字段；5.10 分支只按 ONCS 设 discard 上限、跳过 NVM 命令集 Identify，BUILD_BUG_ON 仅 5.15+ 校验，write-zeroes 上限置 0 |
| `blk_alloc_disk`（**主动发现**） | multipath.c:456 | 5.10 无此 API：按 openEuler 用 `blk_alloc_queue()`+`alloc_disk(0)`+手工挂 queue；保留命名 `snvme%dn%d`，不设 5.13 才有的 QUEUE_FLAG_NOWAIT |
| `device_add_disk`（**主动发现**，返回值） | core.c:3897 | openEuler 5.10 该函数返回 void（带返回值的叫 device_add_disk_safe）；5.10 分支直接调用不判返回，用 `if(0) goto` 保住清理标签避免 -Wunused-label |

> `blk_cleanup_disk`（core.c:3922、multipath.c:848）**不动**——openEuler 5.10 已 backport，可直接用。
> 另在 core.c / pci.c / multipath.c 顶部各加了 `#include <linux/version.h>`。守卫数量：core.c 12、pci.c 2、multipath.c 1。

### 8.3 验证程度
✅ 本机（5.15.0）`make` **零报错**编出两个 .ko——证明所有 `#else`（原 5.15）分支完好、所有 `#if` 守卫语法正确。
⚠️ 5.10 那条分支**无法在本机编译验证**（本机是 5.15），但每处都对照 openEuler 5.10 自己的 nvme 驱动写法，置信度高。

### 8.4 你接下来怎么做（内核移植是"一波波"的）
报错常分多轮：修好一批、往下走又暴露下一批。流程：
1. **我 commit → 你（开发机）push → NPU 服务器 pull**：
   ```bash
   # 开发机
   cd /home/kjx/nds/Geminifs-npu-smoke && git add -A && git commit -m "snvme-5.10-npu: port 5.15->5.10 block APIs (batch 1)" && git push
   # NPU 服务器
   cd <仓库> && git pull
   ```
2. NPU 服务器重编：
   ```bash
   cd backends/local/kernel_modules/snvme-5.10-npu
   make clean && make 2>&1 | tee build.log
   ```
3. **若还有新报错**：把**新报错的符号名**发我（像这次列出即可，不必贴全部输出），我用同样方法继续修。
4. 若编过 → 出现两个 .ko → 进入加载 + 跑测试（本文第 5 节）。

> **为何不一次全修完**：剩余差异要等编译器实际报出来才知道；openEuler 的 backport 让"按版本号猜"不可靠，"报一个改一个"最稳。
>
> **aarch64 提醒（能跑测试时再看）**：⑤ `snvme_smoke_io` 的 `mmio_writel` doorbell 写序，x86 用 `sfence`，arm64 走 `__atomic_thread_fence`（已有分支、能编过）。若 ⑤ CQE 超时/数据偶发错，重点查这条写序在 arm64 的可见性（可能要换 `dma_wmb()` 语义）。与本批内核 API 适配无关，是后话。

### 8.5 batch 2（2026-06-05，报错已大幅减少）

服务器 pull batch 1 后重编，报错明显变少。新修：

| 报错符号 | 文件:位置 | 5.10 改法（依据 openEuler 5.10） |
|---|---|---|
| `disk_update_readahead` | core.c:1990 | 5.15 是 `disk_update_readahead(disk)`；5.10 是 `blk_queue_update_readahead(disk->queue)`（参数是请求队列）。已用版本守卫切换。 |

本机 5.15 回归编译仍通过（两个 .ko 正常产出）。

> 关于"其他看起来像 warning 的输出"：内核模块编译里 `warning:` 一般**不会**中断编译、能正常生成 .ko，可以先不管。**只有当 `make` 最终非 0 退出、或日志里出现 `error:`** 才需要处理——若那样，多半是内核开了 `-Werror`（把告警当错误），把那些 `warning:` 的符号名也发我，我一并用版本守卫修掉。判断是否真的编过，看末尾有没有生成 `snvme-core.ko` 和 `snvme.ko` 即可：
> ```bash
> ls -l snvme-core.ko snvme.ko    # 两个都在 = 编译成功，可以去加载 + 跑测试
> ```

### 8.6 batch 3（2026-06-05）

| 报错 | 文件:位置 | 5.10 改法（依据 openEuler 5.10） |
|---|---|---|
| `nvme_submit_user_cmd` 里 `struct block_device *bdev = ns ? ns->disk->part0 : NULL;` 类型不匹配 | ioctl.c:63 + bio 关联处 | 5.15 的 `gendisk->part0` 是 `struct block_device*`；5.10 的 `gendisk->part0` 是**内嵌的 `struct hd_struct`**（非指针），不能赋给 `block_device*`。5.10 改用 `struct gendisk *udisk = ns ? ns->disk : NULL;`，并把 `bio_set_dev(bio, bdev)` 换成 `bio->bi_disk = udisk;`（5.14 才把 bi_disk/bi_partno 合并成 bi_bdev、引入 bio_set_dev），meta 条件同步改用 `udisk`。 |

ioctl.c 顶部已加 `#include <linux/version.h>`。本机 5.15 回归编译仍通过。

> 注：这类 "part0 / bi_disk / bio_set_dev" 是 5.14 块层把"分区"从 `struct hd_struct` 改成 `struct block_device` 那一大波重构的一部分，是除"磁盘分配(blk_alloc_disk)"之外的第二个差异簇。若后续还有同源报错（如别处再用 `->part0`、`bio_set_dev`、`bdget_disk`、`bdput` 等），发我即可，套路相同。

### 8.7 batch 4（2026-06-05，multipath.c 同源簇）

`multipath.c` 里同属"5.14 块层重构"的几处，一次清完：

| 报错 | 文件:位置 | 5.10 改法（依据 openEuler 5.10） |
|---|---|---|
| `nvme_failover_req`：`bio_set_dev(bio, ns->head->disk->part0)` | multipath.c:90 | → `bio->bi_disk = ns->head->disk;` |
| `nvme_ns_head_submit_bio`：`bio->bi_bdev->bd_disk->private_data`（5.10 的 bio 无 bi_bdev） | multipath.c:318 | → `bio->bi_disk->private_data` |
| 同函数：`bio_set_dev(bio, ns->disk->part0)` | multipath.c:334 | → `bio->bi_disk = ns->disk;` |
| `trace_block_bio_remap` 参数/签名 | multipath.c:336 | 5.10 多首参 q：`trace_block_bio_remap(bio->bi_disk->queue, bio, disk_devt(...), bi_sector)` |

> `nvme_ns_head_open` 里的 `bdev->bd_disk`（multipath.c:358）**不动**——那个 `bdev` 是函数的真实 `struct block_device*` 参数，`bd_disk` 在 5.10 也存在，正常。
>
> 我已主动扫过全模块（core/pci/ioctl/multipath/zns），现在所有 `part0 / bi_bdev / bio_set_dev / trace_block_bio_remap` 用法都已包在版本守卫里、无遗漏。本机 5.15 回归编译仍通过。
>
> 至此两大差异簇——「磁盘分配(blk_alloc_disk/blk_cleanup_disk/disk_update_readahead)」和「分区 block_device 化(part0/bi_disk/bio_set_dev/trace_block_bio_remap)」——应已基本清完，离编过很近了。

### 8.8 batch 5（2026-06-05）：`nv-p2p.h` 桩头与「真实 NVIDIA 头」撞名冲突

你这轮报的是：
```
error: conflicting types for 'nvfs_nvidia_p2p_dma_unmap_pages'
... 在 nvfs-p2p.c 那行 return nvidia_p2p_dma_unmap_pages_p(peer, page_table, dma_mapping); 上报实参/类型不对
```

#### 8.8.1 根因（已实证，不是猜）
这次**不是 5.15→5.10 内核 API 差异**，而是**头文件撞名**：

- 我们的桩头 `nv-p2p.h` 的 include guard 是 `SNVME_NPU_STUB_NV_P2P_H`；
- NVIDIA 官方 `nv-p2p.h` 的 guard 是 `_NV_P2P_H_`——**两者不同**；
- 两份头都定义了**同名结构体** `struct nvidia_p2p_page_table` / `struct nvidia_p2p_dma_mapping` 和对应的 `_t` typedef，但**字段不一样**（真头有 `version/page_size/pages/gpu_uuid…`，桩头只有 `entries`）。

只要机器上能在内核模块的 `-I` 搜索路径里找到一份**真实 NVIDIA `nv-p2p.h`**（装了 NVIDIA 驱动/GDS 的机器就有，如本机 `/usr/src/nvidia-<ver>/nvidia/nv-p2p.h`），两份头会**同时**被预处理器展开 → 同名结构体被定义两次：

```
error: redefinition of 'struct nvidia_p2p_page_table'
error: conflicting types for 'nvidia_p2p_page_table_t' / 'nvidia_p2p_dma_mapping_t'
```

这两条会**级联**成你看到的 `conflicting types for 'nvfs_nvidia_p2p_dma_unmap_pages'`——因为这个包装函数的形参正是 `struct nvidia_p2p_page_table *` / `struct nvidia_p2p_dma_mapping *`，结构体被判成"两个互不兼容的类型"后，函数声明(`.h`)与定义(`.c`)就对不上了；那行 `return nvidia_p2p_dma_unmap_pages_p(...)` 的实参类型报错也是同一处类型混乱的下游表现。

> 为什么我本机（5.15）一直编得过、没报这个？纯属运气：`#include "nv-p2p.h"` 用引号查找**优先命中本目录的桩头**，而真头不在内核构建的搜索路径里。换台把真 `nv-p2p.h` 暴露进 `-I` 的机器（或你服务器上的路径布局）就撞车。

#### 8.8.2 实证（我本机复现 + 验证修复）
我用「假真头（同 tag、不同字段、guard=`_NV_P2P_H_`）」在本机最小复现，确认：
- **旧桩头** + 真头 → 必现 `redefinition of 'struct nvidia_p2p_page_table'` + `conflicting types for 'nvidia_p2p_page_table_t'`（与你报的同一类）。
- **改后桩头** + 真头 → 两种 `#include` 先后顺序都**零冲突**。
- `snvme-5.10-npu` 本机 5.15 正常 `make` 仍**零 error**、两个 `.ko` 照常产出（回归通过）。

#### 8.8.3 改法：给桩头加「与真头共存」的双向守卫（只动 `nv-p2p.h` 一个文件）
把桩头里两个结构体/typedef 的定义，**再套一层 `_NV_P2P_H_` 守卫并主动 `#define _NV_P2P_H_`**：

```c
#ifndef SNVME_NPU_STUB_NV_P2P_H
#define SNVME_NPU_STUB_NV_P2P_H
#include <linux/types.h>

#ifndef _NV_P2P_H_      /* ← 真头的 guard，借用它做共存防护 */
#define _NV_P2P_H_
struct nvidia_p2p_page_table { u32 entries; };
typedef struct nvidia_p2p_page_table nvidia_p2p_page_table_t;
struct nvidia_p2p_dma_mapping { u64 *dma_addresses; };
typedef struct nvidia_p2p_dma_mapping nvidia_p2p_dma_mapping_t;
#endif /* _NV_P2P_H_ */
#endif /* SNVME_NPU_STUB_NV_P2P_H */
```

效果（与 `#include` 先后无关，双向都只剩一份定义）：
- **桩头先被包含**：占住 `_NV_P2P_H_` → 之后真头整段被跳过，不再重复定义（host-only 只用这两个结构体，真头其余声明用不到）。
- **真头先被包含**：`_NV_P2P_H_` 已定义 → 跳过桩头定义，直接用真头那份（真结构体同样有 `entries`/`dma_addresses`，snvme 读取兼容）。

> 这是**纯编译期、与内核版本无关**的改动（和 batch1~4 的 `LINUX_VERSION_CODE` 守卫不同性质），不影响 host-only 运行语义，也不动那段已正确的包装函数代码。

#### 8.8.4 ⚠️ 如果改后这条仍在报——请把"原话"发我
我**没能在你确切的报错文本上 100% 对齐**："conflicting types / redefinition"这条我已实证并修掉；但你转述里"那行 `return ...` 说有 argument（实参/参数过多？）"这一句，单凭转述无法断定是上面类型混乱的级联、还是另有其因（例如服务器上还混进了**签名不同**的另一份 p2p 头）。所以 pull 重编后若这条还在，**请贴这几行原始日志**（不用贴全部）：
```bash
cd backends/local/kernel_modules/snvme-5.10-npu
make clean && make 2>&1 | grep -nE -A3 "conflicting|redefinition|p2p_dma_unmap|nv-p2p" | head -40
# 另外确认服务器上到底有没有真头被搜到：
find / -name nv-p2p.h 2>/dev/null
make 2>&1 | grep -i "nv-p2p.h"        # 看实际命中的是哪份
```
有了这几行我就能精准定位"argument"那条，而不是盲改。

本机 5.15 回归编译仍通过（两个 `.ko` 正常产出）。

### 8.9 batch 6（2026-06-05）：`struct pci_dev` 幽灵作用域 → `incompatible pointer type`（**这才是"argument"那条的真因**）

你 8.8.4 让我要的"原话"补上来了，关键新信息是：
```
error: conflicting types for 'nvfs_nvidia_p2p_dma_unmap_pages'
在 nvidia_p2p_dma_unmap_pages_p(peer, page_table, dma_mapping) 这行，
  对 struct pci_dev *（实参 peer）报 incompatible pointer type
```
→ 这**不是** batch5 的结构体撞名（那条是 `nvidia_p2p_page_table` 之类），而是 **`struct pci_dev` 的"函数原型作用域幽灵类型"**问题。batch5 的共存守卫该留着（防真头撞车），但治不了这条；本批(batch6)才是对症的。

#### 8.9.1 根因（已在本机最小复现实证）
`nvfs-p2p.h` 在它的 typedef / 函数声明里**第一次**用到 `struct pci_dev`，可此前它只 `#include "nv-p2p.h"`（→ `linux/types.h`），**既没 include `pci.h`、也没前置声明 `struct pci_dev`**。

按 C 标准，"在函数原型参数列表里首次出现的 struct 标签"被限定在**该原型的作用域**（function-prototype scope），变成一个和 `linux/pci.h` 里真正的 `struct pci_dev` **不同**的"幽灵类型"。结果：
- `nvfs-p2p.h` 里的 typedef、函数声明 → 形参是**幽灵** `pci_dev *`；
- `nvfs-p2p.c` 定义该函数时，`struct pci_dev` 已被 `module.h/nvfs-core.h` 补成**真实**类型 → **声明(幽灵) ≠ 定义(真实)** → `conflicting types for 'nvfs_nvidia_p2p_dma_unmap_pages'`；
- 函数体里 `nvidia_p2p_dma_unmap_pages_p(peer, …)`：`peer` 是真实 `pci_dev *`，函数指针首参却是幽灵 `pci_dev *` → **`incompatible pointer type`**。

> **为何本机 5.15 一直不报、你 5.10 才报**：5.15 的 `<linux/module.h>`（在 `nvfs-p2p.c` 里**先于** `nvfs-p2p.h` 被包含）会间接先声明出 `struct pci_dev`，于是本头 typedef 命中真实类型、不生幽灵；openEuler 5.10 的 `module.h` 包含链不同，轮到本头时 `struct pci_dev` 还没声明 → 触发幽灵。又是一处"内核版本只是把潜伏 bug 暴露出来"的典型。

#### 8.9.2 实证
- **去掉前置声明的旧头**（模拟现状）独立编译：复现 4 处 `warning: 'struct pci_dev' declared inside parameter list will not be visible …`（对应 unmap/map 两个 typedef + 两个 `nvfs_*` 声明），叠加 `.c` 定义即成 `conflicting types` + `incompatible pointer type`——与你报的完全一致。
- **加了前置声明后**独立编译：**0 警告 0 报错**。
- `snvme-5.10-npu` 本机 5.15 正常 `make` 仍**零 error**、两个 `.ko` 照常产出（回归通过）。

#### 8.9.3 改法：在 `nvfs-p2p.h` 用到它之前，**文件作用域**前置声明一次（只动这一个文件、一行）
```c
#include "nv-p2p.h"

struct pci_dev;     /* [SNVME-NPU batch6] 防"函数原型作用域幽灵类型"，详见头内注释 */

typedef int (*nvidia_p2p_dma_unmap_pages_fptr) (struct pci_dev*, …);
```
这样 typedef、本头所有 `nvfs_*` 函数声明、以及 `.c` 里的定义，引用的都是**同一个文件作用域** `struct pci_dev` 标签；之后 `linux/pci.h` 把它补成完整类型也兼容（这里只用到指针，前置声明就够，不必把 `pci.h` 拖进头里）。纯编译期、与内核版本无关，不改任何运行逻辑。一处声明同时治好 unmap + map 两族（4 个报错点）。

> 同源提示：本头里 `nvidia_p2p_dma_map_pages_fptr` / `nvfs_nvidia_p2p_dma_map_pages` 也用 `struct pci_dev*`，这次的前置声明**一并覆盖**，不会再有遗漏。

pull 重编后若还冒新符号，按老规矩把**原始报错那几行**发我即可。本机 5.15 回归编译仍通过（两个 `.ko` 正常产出）。

### 8.10 batch 7（2026-06-05）：`asm/string_64.h` 不存在（x86 专属头 → aarch64 没有）

报错：
```
fatal error: asm/string_64.h: No such file or directory   （pci.c:32）
```

#### 8.10.1 根因
这条**不是版本问题，是架构问题**。`pci.c` 第 32 行 `#include "asm/string_64.h"`——`string_64.h` 是 **x86_64 专属**的 arch 头（位于内核 `arch/x86/include/asm/`，提供 `memcpy/memset` 等的 x86 实现声明）。原始 `snvme-5.15.0-public/pci.c` 就是这么写的（原作者在 x86 上开发，能编过），本机也是 x86_64 所以一直没暴露。你的 NPU 服务器是 **aarch64**，根本没有 `arch/arm64/include/asm/string_64.h`，于是 fatal error。

#### 8.10.2 改法（只动 pci.c 这一行）
`pci.c` 实际只用到 `memcpy / memset / strcmp` 这些**标准串操作**，它们由架构中立的 `<linux/string.h>` 统一提供（x86_64 和 aarch64 都有）。所以把那行换掉：
```c
/* 原： */ #include "asm/string_64.h"     /* x86_64 专属，aarch64 没有 */
/* 改： */ #include <linux/string.h>      /* 架构中立，全平台可用 */
```
与内核版本无关、与架构无关，x86_64 上同样成立（本机 5.15 回归仍零报错、两个 `.ko` 照常产出）。

> 这是**第三类差异**了：除了「内核版本(batch1~4,6)」「头文件撞名(batch5)」，现在多了「**arch 专属头(batch7)**」。后续若还冒别的 `asm/xxx_64.h` 或 x86 内联汇编（如 `asm/special_insns.h`、`<asm/e820/api.h>`、`rdtsc()`、`clflush` 等 x86-only 写法），都属同一类，发我即可，套路：能换架构中立头就换，换不掉的 x86 内联汇编再按 aarch64 等价写法包版本/架构守卫。
